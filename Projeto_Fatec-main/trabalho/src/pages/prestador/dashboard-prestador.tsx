import { Link } from "react-router"; import { useEffect, useState } from "react"; import { Button } from "../../components/ui/button"; import { Card } from "../../components/ui/card"; import { getSessionUser } from "../../services/auth"; import { getAvailableOrdersForProvider, getOrderById, getProviderProposals, type DemoOrder, type DemoProposal, } from "../../services/demo-orders"; import { getUserRatingStats } from "../../services/rating-stats"; import { Briefcase, DollarSign, TrendingUp, Star, CheckCircle, Send, Award, } from "lucide-react"; function parseBrazilianCurrency(value: string): number { const normalized = value.replace(/[^\d,]/g, "").replace(",", "."); const parsed = Number(normalized); return Number.isFinite(parsed) ? parsed : 0; } function calculateMedian(values: number[]): number { if (values.length === 0) return 0; const sorted = [...values].sort((a, b) => a - b); const middle = Math.floor(sorted.length / 2); if (sorted.length % 2 === 0) { return (sorted[middle - 1] + sorted[middle]) / 2; } return sorted[middle]; } function formatCurrency(value: number): string { return `R$ ${value.toLocaleString("pt-BR", { minimumFractionDigits: 2, maximumFractionDigits: 2, })}`; } export function DashboardPrestador() { const user = getSessionUser(); const [ratingStats, setRatingStats] = useState({ rating: 0, count: 0 }); const [providerProposals, setProviderProposals] = useState<DemoProposal[]>([]); const [completedProposals, setCompletedProposals] = useState<DemoProposal[]>([]); const [availableOpportunities, setAvailableOpportunities] = useState<DemoOrder[]>([]); const providerName = user?.nome?.split(" ")[0] || "Profissional"; useEffect(() => { if (!user?.id) { setProviderProposals([]); setAvailableOpportunities([]); return; } void Promise.all([ getProviderProposals(user.id), getAvailableOrdersForProvider(user.id), ]).then(async ([proposals, opportunities]) => { setProviderProposals(proposals); setAvailableOpportunities(opportunities.slice(0, 3)); const accepted = proposals.filter((proposal) => proposal.status === "accepted"); const completedChecks = await Promise.all( accepted.map(async (proposal) => { const order = await getOrderById(proposal.orderId).catch(() => null); const isCompleted = order?.status === "work_completed_confirmed" || (order?.clientFinishedConfirmed === true && order?.providerFinishedConfirmed === true); return isCompleted ? proposal : null; }) ); setCompletedProposals( completedChecks.filter((proposal): proposal is DemoProposal => proposal !== null) ); }); }, [user?.id]); const sentCount = providerProposals.length; const acceptedCount = providerProposals.filter((proposal) => proposal.status === "accepted").length; const completedCount = completedProposals.length; const acceptanceRate = sentCount > 0 ? Math.round((acceptedCount / sentCount) * 100) : 0; const averageRating = ratingStats.rating > 0 ? ratingStats.rating.toFixed(1) : "0.0"; useEffect(() => { let isMounted = true; if (!user?.id) { setRatingStats({ rating: 0, count: 0 }); return; } void getUserRatingStats(user.id) .then((stats) => { if (isMounted) { setRatingStats({ rating: stats.rating, count: stats.count }); } }) .catch(() => { if (isMounted) { setRatingStats({ rating: 0, count: 0 }); } }); return () => { isMounted = false; }; }, [user?.id]); const totalEarnings = completedProposals.reduce( (sum, proposal) => sum + parseBrazilianCurrency(proposal.myPrice), 0 ); const completedEarningsValues = completedProposals.map((proposal) => parseBrazilianCurrency(proposal.myPrice) ); const averageEarnings = completedCount > 0 ? totalEarnings / completedCount : 0; const medianEarnings = calculateMedian(completedEarningsValues); const stats = [ { label: "Propostas Enviadas", value: String(sentCount), icon: Send, color: "blue", change: sentCount > 0 ? "Ações registradas" : "Nenhuma proposta enviada", }, { label: "Propostas Aceitas", value: String(acceptedCount), icon: CheckCircle, color: "green", change: sentCount > 0 ? `Taxa de ${acceptanceRate}%` : "Sem propostas aceitas", }, { label: "Serviços Concluídos", value: String(completedCount), icon: Briefcase, color: "orange", change: completedCount > 0 ? "Concluídos via aceite" : "Nenhum serviço concluído", }, { label: "Avaliação Média", value: averageRating, icon: Star, color: "yellow", change: ratingStats.count > 0 ? `${ratingStats.count} avaliação(ões)` : "Sem avaliações ainda", }, ]; const earnings = { thisMonth: formatCurrency(totalEarnings), lastMonth: formatCurrency(0), average: formatCurrency(averageEarnings), median: formatCurrency(medianEarnings), total: formatCurrency(totalEarnings), }; return ( <div className="min-h-screen bg-gray-50"> {/* Header */} <div className="bg-blue-700 text-white py-8"> <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"> <div className="flex items-center gap-4 mb-4"> <Briefcase size={40} /> <div> <h1 className="text-3xl font-bold">Olá, {providerName}! 👋</h1> <p className="text-blue-100">Bem-vindo à sua área de trabalho</p> </div> </div> </div> </div> {/* Main Content */} <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"> {/* Quick Action */} <div className="mb-8 flex flex-wrap gap-3"> <Link to="/prestador/servicos-disponiveis"> <Button size="lg" className="bg-orange-600 hover:bg-orange-700 shadow-lg" > <TrendingUp className="mr-2" size={20} /> Ver Serviços Disponíveis </Button> </Link> <Link to="/prestador/avaliacoes"> <Button size="lg" variant="outline" className="border-yellow-500 text-yellow-700 hover:bg-yellow-50" > <Star className="mr-2" size={20} /> Avaliar Clientes </Button> </Link> </div> {/* Stats Grid */} <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8"> {stats.map((stat, index) => ( <Card key={index} className="p-6 hover:shadow-lg transition-shadow"> <div className="flex items-center justify-between mb-4"> <div className={`w-12 h-12 rounded-full flex items-center justify-center ${ stat.color === "blue" ? "bg-blue-100" : stat.color === "green" ? "bg-green-100" : stat.color === "orange" ? "bg-orange-100" : "bg-yellow-100" }`} > <stat.icon className={ stat.color === "blue" ? "text-blue-600" : stat.color === "green" ? "text-green-600" : stat.color === "orange" ? "text-orange-600" : "text-yellow-600" } size={24} /> </div> </div> <p className="text-gray-600 text-sm mb-1">{stat.label}</p> <p className="text-3xl font-bold mb-1">{stat.value}</p> <p className="text-xs text-gray-500">{stat.change}</p> </Card> ))} </div> {/* Earnings Overview */} <Card className="p-6 mb-8 bg-orange-50 border-2 border-orange-200"> <div className="flex items-center gap-3 mb-6"> <div className="bg-orange-600 text-white w-12 h-12 rounded-full flex items-center justify-center"> <DollarSign size={24} /> </div> <h2 className="text-2xl font-bold">Seus Ganhos</h2> </div> <div className="grid md:grid-cols-5 gap-6"> <div> <p className="text-sm text-gray-600 mb-1">Este Mês</p> <p className="text-2xl font-bold text-orange-600">{earnings.thisMonth}</p> </div> <div> <p className="text-sm text-gray-600 mb-1">Mês Anterior</p> <p className="text-2xl font-bold text-gray-700">{earnings.lastMonth}</p> </div> <div> <p className="text-sm text-gray-600 mb-1">Média por Serviço</p> <p className="text-2xl font-bold text-blue-600">{earnings.average}</p> </div> <div> <p className="text-sm text-gray-600 mb-1">Mediana por Serviço</p> <p className="text-2xl font-bold text-indigo-600">{earnings.median}</p> </div> <div> <p className="text-sm text-gray-600 mb-1">Total Ganho</p> <p className="text-2xl font-bold text-orange-600">{earnings.total}</p> </div> </div> </Card> <div className="grid lg:grid-cols-2 gap-8"> {/* Recent Opportunities */} <div> <div className="flex items-center justify-between mb-4"> <h2 className="text-2xl font-bold">Novas Oportunidades</h2> <Link to="/prestador/servicos-disponiveis"> <Button variant="outline" size="sm"> Ver Todas </Button> </Link> </div> <div className="space-y-3"> {availableOpportunities.length === 0 ? ( <Card className="p-6 text-center text-gray-600"> Nenhuma oportunidade nova por enquanto. </Card> ) : ( availableOpportunities.map((opp) => ( <Card key={opp.id} className="p-4 hover:shadow-lg transition-all border-l-4 border-l-orange-500" > <div className="flex items-start justify-between mb-2"> <div> <h3 className="font-bold mb-1">{opp.title}</h3> <p className="text-sm text-gray-600">{opp.category}</p> </div> <span className={`px-2 py-1 rounded-full text-xs font-semibold ${ opp.proposals >= 5 ? "bg-red-100 text-red-800" : opp.proposals >= 2 ? "bg-yellow-100 text-yellow-800" : "bg-green-100 text-green-800" }`} > {opp.proposals >= 5 ? "URGENTE" : opp.proposals >= 2 ? "ALTA" : "NORMAL"} </span> </div> <div className="text-sm text-gray-600 mb-3"> <p>📍 {opp.location}</p> <p className="font-semibold text-orange-600">💰 {opp.budget}</p> </div> <div className="flex items-center justify-between"> <p className="text-xs text-gray-500">Postado em {opp.date}</p> <Link to="/prestador/servicos-disponiveis"> <Button size="sm" className="bg-orange-600 hover:bg-orange-700"> Enviar Orçamento </Button> </Link> </div> </Card> )) )} </div> </div> {/* Performance & Tips */} <div> <h2 className="text-2xl font-bold mb-4">Seu Desempenho</h2> <Card className="p-6 mb-4 bg-orange-50 border-2 border-yellow-200"> <div className="flex items-center gap-3 mb-4"> <div className="bg-yellow-500 text-white w-12 h-12 rounded-full flex items-center justify-center"> <Award size={24} /> </div> <div> <h3 className="font-bold text-lg">Profissional Destaque</h3> <p className="text-sm text-gray-600">Top 10% da plataforma</p> </div> </div> <div className="space-y-2"> <div className="flex items-center justify-between"> <span className="text-sm">Taxa de resposta</span> <span className="font-bold text-green-600">95%</span> </div> <div className="flex items-center justify-between"> <span className="text-sm">Tempo médio de resposta</span> <span className="font-bold text-blue-600">2h</span> </div> <div className="flex items-center justify-between"> <span className="text-sm">Taxa de conclusão</span> <span className="font-bold text-green-600">98%</span> </div> </div> </Card> <Card className="p-6 bg-blue-50 border-2 border-blue-200"> <h3 className="font-bold mb-3 flex items-center gap-2"> <TrendingUp className="text-blue-600" size={20} /> Dicas para Ganhar Mais </h3> <ul className="space-y-2 text-sm text-gray-700"> <li className="flex items-start gap-2"> <span className="text-green-600">✓</span> <span>Responda rápido às novas oportunidades</span> </li> <li className="flex items-start gap-2"> <span className="text-green-600">✓</span> <span>Mantenha seu perfil sempre atualizado</span> </li> <li className="flex items-start gap-2"> <span className="text-green-600">✓</span> <span>Capriche nos detalhes das propostas</span> </li> <li className="flex items-start gap-2"> <span className="text-green-600">✓</span> <span>Peça avaliações aos clientes satisfeitos</span> </li> </ul> </Card> </div> </div> </div> </div> ); } 