import { useEffect, useState } from "react"; import { Link } from "react-router"; import { Button } from "../components/ui/button"; import { Card } from "../components/ui/card"; import { Input } from "../components/ui/input"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue, } from "../components/ui/select"; import { Search, Filter, Calendar, MapPin, DollarSign, Star } from "lucide-react"; import { getSessionUser } from "../services/auth"; import { getClientOrders, type DemoOrder } from "../services/demo-orders"; export function Historico() { const [filterCategory, setFilterCategory] = useState("all"); const [searchQuery, setSearchQuery] = useState(""); const [completedOrders, setCompletedOrders] = useState<DemoOrder[]>([]); const [loading, setLoading] = useState(true); const user = getSessionUser(); useEffect(() => { if (!user?.id) { setLoading(false); return; } void getClientOrders(user.id).then((orders) => { // Filtrar apenas pedidos com status work_completed_confirmed
 const completed = orders.filter((order) => order.status === "work_completed_confirmed"); setCompletedOrders(completed); setLoading(false); }); }, [user?.id]); const filteredOrders = completedOrders.filter((order) => { const matchesCategory = filterCategory === "all" || order.category === filterCategory; const matchesSearch = order.title.toLowerCase().includes(searchQuery.toLowerCase()) || order.category.toLowerCase().includes(searchQuery.toLowerCase()); return matchesCategory && matchesSearch; }); if (loading) { return ( <div className="min-h-screen bg-gray-50"> <div className="bg-blue-700 text-white py-8"> <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"> <div className="h-9 w-64 rounded bg-blue-600 animate-pulse mb-2" /> <div className="h-4 w-40 rounded bg-blue-600/50 animate-pulse" /> </div> </div> <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-4"> {[1,2,3].map((i) => ( <Card key={i} className="p-6 animate-pulse"> <div className="h-6 w-48 rounded bg-gray-200 mb-3" /> <div className="h-4 w-full rounded bg-gray-200 mb-2" /> <div className="h-4 w-3/4 rounded bg-gray-200" /> </Card> ))} </div> </div> ); } return ( <div className="min-h-screen bg-gray-50"> {/* Header */} <div className="bg-blue-700 text-white py-8"> <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"> <h1 className="text-3xl font-bold mb-2">Histórico de Serviços</h1> <p className="text-blue-100">Serviços realizados e avaliações</p> </div> </div> {/* Main Content */} <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8"> {/* Filters */} <div className="bg-white rounded-lg shadow-md p-4 mb-6"> <div className="flex flex-col md:flex-row gap-4"> <div className="flex-1 relative"> <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400" size={20} /> <Input type="text" placeholder="Buscar serviços..." value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} className="pl-10" /> </div> <Select value={filterCategory} onValueChange={setFilterCategory}> <SelectTrigger className="w-full md:w-48"> <Filter size={16} className="mr-2" /> <SelectValue placeholder="Categoria" /> </SelectTrigger> <SelectContent> <SelectItem value="all">Todas Categorias</SelectItem> </SelectContent> </Select> </div> </div> {/* Results Count */} <div className="mb-4"> <p className="text-gray-600"> <span className="font-semibold">{filteredOrders.length}</span> serviço {filteredOrders.length !== 1 ? "s" : ""} concluído {filteredOrders.length !== 1 ? "s" : ""} </p> </div> {/* Services List */} <div className="space-y-4"> {filteredOrders.map((order) => ( <Card key={order.id} className="p-6 hover:shadow-xl transition-all border-2 border-green-200 bg-green-50/30" > <div className="flex flex-col lg:flex-row gap-6"> {/* Main Info */} <div className="flex-1"> <div className="flex items-start justify-between mb-3"> <div> <h3 className="text-xl font-bold mb-1">{order.title}</h3> <p className="text-gray-600 text-sm">{order.category}</p> </div> <span className="px-3 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800 border border-green-300"> Concluído </span> </div> <p className="text-gray-700 mb-4">{order.description}</p> <div className="flex flex-wrap gap-4 text-sm text-gray-600"> <div className="flex items-center gap-1"> <Calendar size={16} /> <span>{order.date}</span> </div> <div className="flex items-center gap-1"> <MapPin size={16} /> <span>{order.location}</span> </div> <div className="flex items-center gap-1"> <DollarSign size={16} /> <span>{order.budget}</span> </div> </div> </div> {/* Actions */} <div className="lg:w-48 flex flex-col gap-3"> <div className="bg-emerald-50 p-4 rounded-lg text-center border-2 border-green-200"> <div className="text-3xl font-bold bg-emerald-600 bg-clip-text text-transparent"> ✓ </div> <div className="text-sm text-gray-600 mt-1">Serviço Concluído</div> </div> <Link to={`/dashboard/pedido/${order.id}`}> <Button className="w-full bg-emerald-600 hover:bg-emerald-700"> <Star size={16} className="mr-2" /> Avaliar Prestador </Button> </Link> </div> </div> </Card> ))} </div> {/* Empty State */} {filteredOrders.length === 0 && ( <Card className="p-12 text-center"> <div className="text-gray-400 mb-4"> <Search size={64} className="mx-auto" /> </div> <h3 className="text-xl font-semibold mb-2"> Nenhum serviço concluído ainda </h3> <p className="text-gray-600 mb-6"> Assim que você concluir serviços com sucesso, eles aparecerão aqui. </p> <Link to="/dashboard/meus-pedidos"> <Button className="bg-blue-500 hover:bg-blue-600"> Ver Meus Pedidos </Button> </Link> </Card> )} </div> </div> ); } 